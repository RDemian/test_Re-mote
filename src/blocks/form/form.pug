mixin form(modifier)
  form.form&attributes(attributes)
    h2.text-center.mb-30 Форма
    input.input.mb-20(type="text", placeholder="Имя")
    input.input.mb-20(type="email", placeholder="Email")
    textarea.input.input--textarea.mb-20(placeholder="Сообщение", rows="5")
    button.button.mt-10(type="submit") Отправить